Sort(+n)
  Union(2 inputs)
    Aggregate(count() as n)
      Filter((level == "ERROR"))
        Scan(main)
          pushdown.field_predicate: (level == "ERROR")
    Aggregate(count() as n)
      Filter((level == "WARN"))
        Scan(main)
          pushdown.field_predicate: (level == "WARN")
